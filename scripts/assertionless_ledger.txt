# Known assertion-less `#[test]` functions (#2818).
#
# `scripts/assertionless_tests.py --ledger scripts/assertionless_ledger.txt`
# ratchets the tree against this file and fails in BOTH directions: an offender
# that is not recorded here is a regression, and an entry here that now asserts
# something is a stale ledger whose line must be deleted in the fix's own commit.
# So this list only ever shrinks, and it is also the detector's positive control
# on every run -- every line is a known-offending input, and a scan that silently
# measured nothing reports them all as fixed.
#
# Sorted, unique, one `path::fn_name` per line. Do not add to it: the way past
# this gate is to assert the invariant the test already computes, or to move a
# pure exploratory dump to `examples/`.
#
# Each entry below prints a measurement and asserts nothing about it, so it
# passes for every behaviour of the code it calls.
crates/gam-models/tests/suite/probe_2695_warp_smoothness.rs::probe_2695_warp_basis_smoothness_ladder
